version: '2'

services:
  tezster-cli:
    image: tezster-docker5
    ports:
      - "0.0.0.0:18731:18731"
    network_mode: host

    command: bash -c "cd /usr/local/bin && start_nodes.sh && tail -f /dev/null"
     